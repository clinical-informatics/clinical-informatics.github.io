"""Track 05: Financial management for informaticists (scaffold).

This notebook is a placeholder. Content will be added as the curriculum
builds out. See `tasks.md` in the curriculum root for current status.
"""

import marimo

__generated_with = "0.23.6"
app = marimo.App(width="medium")


@app.cell
def _():
    import marimo as mo
    return (mo,)


@app.cell
def _(mo):
    mo.md(
        r"""
        # Track 05: Financial management for informaticists

        This track is **scaffolded only**. The interactive notebook will be filled in during a later phase of the curriculum build-out.

        Check `tasks.md` in the curriculum root for current status. The track's `README.md` in this folder has the one-paragraph description of what this notebook will cover when it's built.
        """
    )
    return


if __name__ == "__main__":
    app.run()
